# Быстрый старт

Это краткое руководство поможет запустить бота за 10 минут.

## 1. Установка зависимостей

```bash
# Создание виртуального окружения
python -m venv venv

# Активация (выберите вашу ОС)
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка пакетов
pip install -r requirements.txt
```

## 2. Создание Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## 3. Получение вашего Telegram ID

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID (например: `123456789`)

## 4. Создание Solana кошелька

```bash
python create_wallet.py
```

Сохраните публичный и приватный ключи!

## 5. Настройка .env файла

Создайте файл `.env` со следующим содержимым:

```env
# Telegram
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id

# Database
DATABASE_URL=sqlite+aiosqlite:///./bot.db

# Solana (для тестирования используйте devnet)
SOLANA_RPC_URL=https://api.devnet.solana.com
MASTER_WALLET_PUBLIC_KEY=публичный_ключ_из_create_wallet
MASTER_WALLET_PRIVATE_KEY=приватный_ключ_из_create_wallet

# Settings
MIN_DEPOSIT_SOL=0.01
IMAGE_PRICE_SOL=0.05
WITHDRAWAL_FEE_PERCENT=2
```

## 6. Проверка настройки

```bash
python test_connection.py
```

Все тесты должны быть зелеными ✓

## 7. Инициализация базы данных

```bash
python init_db.py
```

Эта команда создаст таблицы и добавит регионы ЕС с городами.

## 8. Получение тестовых SOL (для devnet)

1. Перейдите на [https://faucet.solana.com/](https://faucet.solana.com/)
2. Вставьте ваш `MASTER_WALLET_PUBLIC_KEY`
3. Нажмите "Airdrop"
4. Подождите несколько секунд

Или используйте Solana CLI:
```bash
solana airdrop 2 ваш_публичный_ключ --url https://api.devnet.solana.com
```

## 9. Запуск бота

Откройте **два** терминала:

**Терминал 1 (основной бот):**
```bash
python main.py
```

**Терминал 2 (монитор транзакций):**
```bash
python monitor_transactions.py
```

## 10. Тестирование

1. Найдите вашего бота в Telegram (по username из BotFather)
2. Отправьте `/start`
3. Вы должны увидеть приветственное сообщение с вашим кошельком

### Что можно протестировать:

**Как пользователь:**
- [ ] `/start` - приветствие
- [ ] `📍 Выбрать регион` - выбор локации
- [ ] `🛍 Каталог` - просмотр товаров (пока пусто)
- [ ] `💰 Мой баланс` - информация о балансе
- [ ] `ℹ️ Помощь` - справка

**Как администратор:**
- [ ] `/admin` - открыть админ-панель
- [ ] `➕ Добавить товар` - загрузить изображение
- [ ] `📊 Статистика` - просмотр статистики
- [ ] `🗂 Управление регионами` - управление локациями

## Пример: Добавление товара

1. В боте: `/admin`
2. Нажмите `➕ Добавить товар`
3. Выберите регион (например: `/1` для Germany)
4. Выберите город (например: `/1` для Berlin)
5. Отправьте любое изображение
6. Введите цену (например: `0.05`)
7. Введите описание или `-` чтобы пропустить
8. Готово! Товар добавлен

## Пример: Покупка товара

1. Выберите `📍 Выбрать регион`
2. Выберите регион и город где добавили товар
3. Перейдите в `🛍 Каталог`
4. Выберите товар
5. Нажмите `💳 Купить`
6. Подтвердите покупку

⚠️ **Примечание**: Для покупки нужен баланс. Пополните его:
- Переведите SOL на адрес из раздела `💰 Мой баланс`
- Подождите ~30 секунд пока монитор обработает транзакцию

## Структура проекта

```
bot2/
├── main.py                      # Главный файл бота
├── monitor_transactions.py      # Мониторинг транзакций
├── init_db.py                   # Инициализация БД
├── create_wallet.py             # Создание кошелька
├── test_connection.py           # Тест подключения
├── config.py                    # Конфигурация
├── requirements.txt             # Зависимости
├── .env                         # Настройки (создайте сами)
├── README.md                    # Полная документация
├── QUICKSTART.md               # Этот файл
├── SOLANA_SETUP.md             # Настройка Solana
├── DEPLOYMENT.md               # Развертывание на сервере
│
├── database/                   # База данных
├── services/                   # Бизнес-логика
├── handlers/                   # Обработчики команд
├── middleware/                 # Middleware
└── utils/                      # Утилиты
```

## Часто возникающие проблемы

### Ошибка: "Module not found"

```bash
# Убедитесь что виртуальное окружение активно
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt
```

### Ошибка: "Bot token is invalid"

- Проверьте `BOT_TOKEN` в `.env`
- Убедитесь что нет пробелов в начале/конце
- Получите новый токен от @BotFather если нужно

### Ошибка: "Connection to Solana failed"

- Проверьте интернет-соединение
- Попробуйте другой RPC URL
- Используйте devnet для тестирования

### Бот не отвечает

- Убедитесь что `main.py` запущен
- Проверьте логи в терминале
- Попробуйте `/start` снова

### Баланс не обновляется

- Убедитесь что `monitor_transactions.py` запущен
- Подождите 30-60 секунд после перевода
- Проверьте что перевели на правильный адрес
- Проверьте сеть (devnet/mainnet должны совпадать)

### Не могу добавить товар

- Убедитесь что ваш ID в `ADMIN_IDS`
- Сначала добавьте регионы через `init_db.py`
- Проверьте права на директорию `images/`

## Переход на продакшен

Когда будете готовы к реальному использованию:

1. **Создайте новый кошелек** для продакшена
2. **Измените RPC на mainnet**:
   ```env
   SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
   ```
3. **Пополните мастер-кошелек** реальными SOL
4. **Используйте PostgreSQL** вместо SQLite:
   ```env
   DATABASE_URL=postgresql+asyncpg://user:pass@localhost/botdb
   ```
5. **Настройте сервер** согласно `DEPLOYMENT.md`
6. **Используйте приватный RPC** (QuickNode, Alchemy)
7. **Настройте мониторинг** и алерты
8. **Регулярно делайте бэкапы**

## Полезные команды

```bash
# Остановить бота: Ctrl+C в терминале

# Просмотр логов (если используете systemd)
sudo journalctl -u bot.service -f

# Перезапуск бота
# Остановите (Ctrl+C) и запустите снова
python main.py

# Очистка базы (⚠️ УДАЛИТ ВСЕ ДАННЫЕ)
rm bot.db
python init_db.py

# Проверка баланса через CLI
solana balance ваш_публичный_ключ --url https://api.devnet.solana.com
```

## Дальнейшие шаги

- 📖 Прочитайте `README.md` для полной документации
- 🔐 Изучите `SOLANA_SETUP.md` для настройки Solana
- 🚀 Прочитайте `DEPLOYMENT.md` для развертывания
- 🎨 Кастомизируйте текст в `handlers/` файлах
- 🌍 Добавьте свои регионы/города через админ-панель

## Поддержка

- 📝 Создавайте Issues на GitHub
- 💬 Задавайте вопросы в Discussions
- 📧 Связь: your-email@example.com

---

**Готово!** 🎉 Ваш бот запущен и готов к использованию!

Не забудьте:
- ⭐ Поставить звезду проекту на GitHub
- 🔒 Держать `.env` в секрете
- 💾 Делать регулярные бэкапы
- 🔄 Обновлять зависимости

