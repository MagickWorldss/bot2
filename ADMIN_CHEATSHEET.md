# 👑 Шпаргалка администратора

## 🚀 Быстрый доступ

### Открыть админ-панель:
```
/admin
```

---

## 📋 Главное меню

После `/admin` вы видите 5 кнопок:

| Кнопка | Действие |
|--------|----------|
| ➕ Добавить товар | Загрузить новое изображение |
| 📊 Статистика | Просмотр статистики бота |
| 🗂 Управление регионами | Регионы и города |
| 👥 Управление пользователями | Список пользователей |
| 🔙 Главное меню | Выход из админки |

---

## 1️⃣ Добавить товар (5 шагов)

```
1. Выбрать регион       → /1, /2, /3...
2. Выбрать город        → /1, /2, /3...
3. Отправить фото       → [загрузить изображение]
4. Указать цену         → 0.05
5. Добавить описание    → текст или -
```

**Результат**: ✅ Товар добавлен!

---

## 2️⃣ Статистика

Показывает:
- 👥 Количество пользователей
- 🖼 Всего товаров
- ✅ Продано товаров
- 📦 Доступно товаров
- 💰 Общая выручка в SOL

---

## 3️⃣ Управление регионами

### Просмотр:
```
📍 Управление регионами
  ✅ Germany
  ✅ France
  ❌ Spain
  ➕ Добавить регион
```

### Действия с регионом:
```
[Выбрать регион]
  🏙 Управление городами
  🔴 Деактивировать
  🗑 Удалить регион
```

### Добавить регион:
```
1. Название → Germany
2. ISO код → DE
✅ Регион добавлен!
```

### Добавить город:
```
[Регион] → 🏙 Управление городами → ➕ Добавить город
Название → Munich
✅ Город добавлен!
```

---

## 4️⃣ Управление пользователями

Показывает список:
```
✅ ID: 123456789
   Имя: John
   Username: @john_doe
   Баланс: 0.15 SOL
```

---

## ⚡ Быстрые команды

### Для бота:
```
/start              - главное меню
/help               - помощь
/admin              - админ-панель
```

### Для проверки (терминал):
```bash
# Логи
docker-compose logs -f bot

# Статус
docker-compose ps

# Перезапуск
docker-compose restart bot
```

---

## 🎯 Типичные сценарии

### Добавить 10 товаров в Berlin:

```
/admin
➕ Добавить товар
/1 (Germany)
/1 (Berlin)
[фото 1] → 0.05 → описание 1
[фото 2] → 0.05 → описание 2
...
```

### Добавить новый регион с городами:

```
/admin
🗂 Управление регионами
➕ Добавить регион
Switzerland → CH
🏙 Управление городами
➕ Добавить город → Zurich
➕ Добавить город → Geneva
```

### Проверить что продается:

```
/admin
📊 Статистика
→ смотрим количество доступных товаров
```

---

## 🛠 Прямой доступ к БД

### Через Docker:
```bash
docker-compose exec bot python
```

```python
from database.database import db
from services.image_service import ImageService
import asyncio

async def check():
    async for session in db.get_session():
        images = await ImageService.get_available_images(session)
        print(f"Доступно товаров: {len(images)}")

asyncio.run(check())
```

### Изменить цену:
```python
async def change_price(image_id, new_price):
    async for session in db.get_session():
        img = await ImageService.get_image_by_id(session, image_id)
        img.price_sol = new_price
        await session.commit()
```

### Добавить баланс:
```python
from services.user_service import UserService

async def add_money(user_id, amount):
    async for session in db.get_session():
        await UserService.update_balance(session, user_id, amount)
```

---

## ⚠️ Важно помнить

✅ **Делайте бэкапы**: `tar -czf backup.tar.gz data/ images/ .env`  
✅ **Проверяйте логи**: `docker-compose logs -f`  
✅ **Мониторьте баланс**: мастер-кошелек должен иметь SOL  
✅ **Тестируйте на devnet**: перед mainnet  
⚠️ **Не удаляйте регионы** с активными товарами  
⚠️ **Цены в SOL**: учитывайте комиссии сети  

---

## 📞 Быстрая помощь

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | `docker-compose restart bot` |
| Нет товаров | Добавьте через админку |
| Нет регионов | Запустите `init_db.py` |
| Ошибки в логах | `docker-compose logs bot` |
| База не работает | `docker-compose restart db` |

---

## 🔗 Полная документация

- 📖 **`ADMIN_GUIDE.md`** - полное руководство администратора
- 📖 **`README.md`** - общая документация
- 📖 **`START_HERE.md`** - быстрый старт

---

**Версия**: 1.0.0  
**Последнее обновление**: Октябрь 2024

