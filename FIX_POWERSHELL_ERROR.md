# 🔧 Исправление ошибки PowerShell на Windows

## ❌ Ошибка: "выполнение сценариев отключено в этой системе"

Это стандартная защита Windows. Есть 3 простых решения!

---

## ✅ РЕШЕНИЕ 1: Использовать cmd (самое простое!)

### Вместо PowerShell используйте обычную командную строку:

1. **Нажмите**: Win + R
2. **Введите**: `cmd`
3. **Нажмите**: Enter
4. **Перейдите в папку**:
   ```cmd
   cd C:\dev\tg\bot2
   ```
5. **Выполните команды**:
   ```cmd
   railway login
   railway link
   railway run python init_db.py
   ```

**Готово!** ✅

---

## ✅ РЕШЕНИЕ 2: Разрешить выполнение (один раз)

### Откройте PowerShell от администратора:

1. **Нажмите**: Win + X
2. **Выберите**: "Windows PowerShell (Администратор)" или "Терминал (Администратор)"
3. **Подтвердите**: "Да" (UAC запрос)
4. **Выполните команду**:
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
5. **Подтвердите**: нажмите `Y` и Enter

Теперь PowerShell будет работать!

### Проверка:

```powershell
cd C:\dev\tg\bot2
railway login
```

Должно работать! ✅

---

## ✅ РЕШЕНИЕ 3: Обход для текущей сессии

### В обычном PowerShell:

```powershell
# Разрешить для текущей сессии
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process

# Теперь выполните команды
railway login
railway link
railway run python init_db.py
```

Это работает только для текущего окна PowerShell.

---

## ✅ РЕШЕНИЕ 4: Использовать npx (без установки railway CLI)

```powershell
# Вместо: railway login
# Используйте:
npx @railway/cli login

# Вместо: railway link
npx @railway/cli link

# Вместо: railway run python init_db.py
npx @railway/cli run python init_db.py
```

---

## 🎯 РЕКОМЕНДАЦИЯ

**Самое простое - РЕШЕНИЕ 1 (cmd)**

Просто используйте обычную командную строку вместо PowerShell!

```
Win + R → cmd → Enter
cd C:\dev\tg\bot2
railway login
railway link
railway run python init_db.py
```

**Работает сразу без дополнительных настроек!** ✅

---

## 📝 Команды для инициализации БД

После того как исправите ошибку PowerShell:

### В cmd или PowerShell:

```bash
# 1. Войти в Railway
railway login

# 2. Подключиться к проекту
railway link

# 3. Инициализировать БД
railway run python init_db.py
```

Должно показать:
```
✓ Created region: Germany
  ✓ Created city: Berlin
  ...
✅ Database initialization complete!
```

---

## ✅ После инициализации

Всё готово! Бот полностью работает!

### Проверьте:

1. **Telegram** → найдите `@astra_shop_bot`
2. **Отправьте**: `/start`
3. **Отправьте**: `/admin`
4. **Выберите регион**: 📍 Выбрать регион
5. **Посмотрите каталог**: 🛍 Каталог

Всё должно работать! 🎉

---

## 🆘 Если всё равно не работает

Попробуйте все 4 решения по порядку:
1. cmd ✅ (самое простое)
2. PowerShell от админа
3. Bypass для сессии
4. npx railway

Хотя бы одно точно сработает! 💪

---

**Файл создан**: FIX_POWERSHELL_ERROR.md  
**Рекомендация**: Используйте cmd (РЕШЕНИЕ 1)

