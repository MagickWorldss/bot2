# 🌐 Запуск на хостинге - ПОШАГОВАЯ ИНСТРУКЦИЯ

## 🎯 Что нужно сделать

Перенести бота с локальной машины на сервер, чтобы он работал 24/7.

## ⚡ БЫСТРЫЙ СПОСОБ (5 минут)

### Шаг 1: Получите VPS сервер

**Рекомендуем DigitalOcean** ($6/месяц):

1. Зарегистрируйтесь: https://www.digitalocean.com
2. Create → Droplets
3. Выберите:
   - **Ubuntu 22.04 LTS**
   - **Basic Plan** - $6/мес (1GB RAM)
   - **Регион** - ближайший к вам
4. Создайте Droplet
5. Получите IP адрес (например: `123.45.67.89`)

**Альтернативы:**
- Hetzner (€4/мес): https://www.hetzner.com
- Vultr ($6/мес): https://www.vultr.com
- AWS Free Tier (бесплатно год): https://aws.amazon.com

### Шаг 2: Загрузите проект на сервер

**Вариант A: Через Git (если проект в GitHub)**

```bash
# На сервере
ssh root@ваш_ip

# Установите Git
apt update
apt install git -y

# Клонируйте проект
git clone https://github.com/ваш_username/bot2.git
cd bot2
```

**Вариант B: Через SCP (прямая загрузка)**

```bash
# На вашей локальной машине (в папке bot2)
# Linux/Mac:
chmod +x upload_to_server.sh
./upload_to_server.sh

# Введите IP сервера, пользователя и готово!
```

**Вариант C: Вручную через FileZilla/WinSCP**

1. Скачайте FileZilla: https://filezilla-project.org
2. Подключитесь: SFTP, ваш_ip, root, пароль
3. Загрузите всю папку bot2

### Шаг 3: Настройте сервер

```bash
# Подключитесь к серверу
ssh root@ваш_ip

# Перейдите в папку проекта
cd bot2

# Установите Docker (один раз)
curl -fsSL https://get.docker.com | sh

# Установите Docker Compose (один раз)
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# Проверьте установку
docker --version
docker-compose --version
```

### Шаг 4: Создайте .env файл

```bash
# На сервере, в папке bot2
nano .env
```

Вставьте (замените на свои данные):

```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id

DATABASE_URL=postgresql+asyncpg://botuser:changeme@db:5432/botdb
POSTGRES_DB=botdb
POSTGRES_USER=botuser
POSTGRES_PASSWORD=changeme

SOLANA_RPC_URL=https://api.devnet.solana.com
MASTER_WALLET_PUBLIC_KEY=ваш_публичный_ключ
MASTER_WALLET_PRIVATE_KEY=ваш_приватный_ключ

MIN_DEPOSIT_SOL=0.01
IMAGE_PRICE_SOL=0.05
WITHDRAWAL_FEE_PERCENT=2
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

### Шаг 5: Запустите бота

```bash
# Сделайте скрипт исполняемым
chmod +x deploy.sh

# Запустите
./deploy.sh
```

В меню выберите: `1` (Полное развертывание)

**Готово! Бот запущен! 🎉**

### Шаг 6: Проверьте работу

```bash
# Проверьте что контейнеры запущены
docker-compose ps

# Смотрите логи
docker-compose logs -f bot

# Для выхода из логов: Ctrl+C
```

Откройте Telegram → найдите бота → отправьте `/start`

## 📱 ЕЩЕ ПРОЩЕ: Railway.app (без VPS)

Если не хотите возиться с сервером:

1. Зарегистрируйтесь: https://railway.app
2. New Project → Deploy from GitHub
3. Подключите ваш репозиторий
4. Variables → добавьте переменные из .env
5. Deploy!

**Цена**: $5/месяц, автоматически все настраивается.

## 🔧 Управление ботом на сервере

### Подключение к серверу

```bash
ssh root@ваш_ip
cd bot2
```

### Просмотр логов

```bash
# Логи бота
docker-compose logs -f bot

# Логи монитора транзакций
docker-compose logs -f monitor

# Все логи
docker-compose logs -f
```

### Перезапуск бота

```bash
docker-compose restart bot
```

### Остановка бота

```bash
docker-compose down
```

### Обновление бота

```bash
# Получить изменения
git pull

# Пересобрать и перезапустить
docker-compose down
docker-compose build
docker-compose up -d
```

### Бэкап данных

```bash
# Создать бэкап
tar -czf backup_$(date +%Y%m%d).tar.gz data/ images/ .env

# Скачать на локальную машину
# На вашем компьютере:
scp root@ваш_ip:/root/bot2/backup_*.tar.gz ./
```

## 🎛 Полезные команды

```bash
# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# Удалить старые образы (освободить место)
docker system prune -a

# Вход в контейнер бота
docker-compose exec bot /bin/bash
```

## 🔒 Безопасность

### 1. Смените пароль root

```bash
passwd
```

### 2. Создайте нового пользователя (опционально)

```bash
adduser botuser
usermod -aG sudo botuser
su - botuser
```

### 3. Настройте firewall

```bash
# Установите UFW
apt install ufw -y

# Разрешите SSH
ufw allow 22

# Включите
ufw enable

# Проверьте
ufw status
```

### 4. Регулярные обновления

```bash
apt update && apt upgrade -y
```

## 💰 Переход с devnet на mainnet

Когда готовы к реальным деньгам:

1. **Создайте новый кошелек**
   ```bash
   # На локальной машине
   python create_wallet.py
   ```

2. **Обновите .env на сервере**
   ```bash
   nano .env
   ```
   
   Измените:
   ```env
   SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
   MASTER_WALLET_PUBLIC_KEY=новый_ключ
   MASTER_WALLET_PRIVATE_KEY=новый_ключ
   IMAGE_PRICE_SOL=0.1  # или другая реальная цена
   ```

3. **Пополните мастер-кошелек**
   - Купите SOL на бирже
   - Переведите на ваш публичный ключ

4. **Перезапустите бота**
   ```bash
   docker-compose restart
   ```

## 🆘 Проблемы и решения

### Бот не запускается

```bash
# Проверьте логи
docker-compose logs bot

# Проверьте .env
cat .env

# Перезапустите
docker-compose restart bot
```

### Забыли IP сервера

Зайдите в панель вашего хостинга (DigitalOcean, Hetzner и т.д.)

### Забыли пароль от сервера

В панели хостинга есть опция "Reset Root Password" или "Console Access"

### Нехватка места на диске

```bash
# Проверьте место
df -h

# Очистите Docker
docker system prune -a

# Удалите старые логи
rm *.log
```

### База данных не работает

```bash
# Остановите все
docker-compose down

# Удалите volumes
docker-compose down -v

# Запустите заново
docker-compose up -d

# Переинициализируйте БД
docker-compose exec bot python init_db.py
```

## 📊 Мониторинг

### Проверка здоровья бота

```bash
# Статус всех контейнеров
docker-compose ps

# Должно быть: State = Up
```

### Проверка использования ресурсов

```bash
# CPU, RAM, Network
docker stats

# Место на диске
df -h
```

### Автоматические алерты (опционально)

Установите Uptime Robot: https://uptimerobot.com
- Бесплатный мониторинг
- Email уведомления при падении

## 🎓 Пошаговое видео (если нужно)

Полная документация:
- `QUICK_DEPLOY.md` - упрощенная версия
- `HOSTING_GUIDE.md` - детальная информация
- `DEPLOYMENT.md` - продакшен развертывание

## ✅ Чеклист

- [ ] VPS сервер получен (IP адрес записан)
- [ ] Проект загружен на сервер
- [ ] Docker и Docker Compose установлены
- [ ] .env файл создан и заполнен
- [ ] `./deploy.sh` выполнен успешно
- [ ] `docker-compose ps` показывает "Up"
- [ ] Логи не содержат ошибок
- [ ] Бот отвечает в Telegram
- [ ] `/admin` работает
- [ ] Мониторинг транзакций запущен

## 🎉 Готово!

Ваш бот теперь работает 24/7 на сервере!

**Важно:**
- Запишите IP адрес сервера
- Сохраните .env файл в безопасном месте
- Регулярно проверяйте логи
- Делайте бэкапы еженедельно

**Для отключения от сервера:**
```bash
exit
```

**Бот продолжит работать в фоне!** 🚀

Удачи! 💪

---

**Нужна помощь?**
- Смотрите `QUICK_DEPLOY.md`
- Читайте `HOSTING_GUIDE.md`
- Создайте Issue на GitHub

