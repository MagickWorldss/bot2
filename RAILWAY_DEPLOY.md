# 🚂 Развертывание на Railway.app

## 🎯 Самый простой способ запустить бота 24/7!

Railway.app - это PaaS платформа, которая автоматически разворачивает ваш бот. Не нужно настраивать VPS, Docker вручную - все делается автоматически!

---

## ⚡ БЫСТРЫЙ СТАРТ (5 минут)

### Шаг 1: Подготовка проекта

✅ Уже готово! Все файлы настроены для Railway.

### Шаг 2: Push в GitHub

```bash
# Если еще не инициализирован Git
git init

# Добавьте все файлы
git add .

# Коммит
git commit -m "Initial commit for Railway deployment"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш_username/bot2.git

# Push
git push -u origin main
```

### Шаг 3: Развертывание на Railway

1. **Зайдите на [Railway.app](https://railway.app)**

2. **Войдите через GitHub**
   - Sign up / Login → Continue with GitHub

3. **Создайте новый проект**
   - New Project → Deploy from GitHub repo
   - Выберите ваш репозиторий `bot2`

4. **Railway автоматически определит Dockerfile**
   - Начнется сборка образа
   - Это займет 2-3 минуты

5. **Добавьте переменные окружения**
   - Перейдите в Settings → Variables
   - Добавьте следующие переменные:

```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway
SOLANA_RPC_URL=https://api.devnet.solana.com
MASTER_WALLET_PUBLIC_KEY=ваш_публичный_ключ
MASTER_WALLET_PRIVATE_KEY=ваш_приватный_ключ
MIN_DEPOSIT_SOL=0.01
IMAGE_PRICE_SOL=0.05
WITHDRAWAL_FEE_PERCENT=2
```

6. **Добавьте PostgreSQL базу данных**
   - New → Database → Add PostgreSQL
   - Railway автоматически создаст БД и DATABASE_URL

7. **Настройте Worker (монитор транзакций)**
   - Settings → Deploy → Add Service
   - Выберите тот же репозиторий
   - В Settings → Start Command: `python monitor_transactions.py`

8. **Deploy!**
   - Railway автоматически задеплоит
   - Через 2-3 минуты бот будет онлайн!

### Шаг 4: Инициализация базы данных

После первого деплоя нужно один раз инициализировать БД:

1. В Railway → Settings → открыть консоль (или локально):

```bash
# Если локально с Railway CLI
railway run python init_db.py

# Или через веб-консоль Railway
python init_db.py
```

### Шаг 5: Проверка

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должно прийти приветствие!

---

## 📋 Полная инструкция

### 1. Создание кошелька (если еще нет)

```bash
# На локальной машине
python create_wallet.py
```

Сохраните публичный и приватный ключи!

### 2. Получение BOT_TOKEN

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

### 3. Получение ADMIN_IDS

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID

### 4. Подготовка GitHub репозитория

```bash
# Инициализация (если нужно)
git init

# Добавление файлов
git add .

# Коммит
git commit -m "Ready for Railway deployment"

# Создайте репозиторий на GitHub
# https://github.com/new

# Добавьте remote
git remote add origin https://github.com/ваш_username/bot2.git

# Push
git push -u origin main
```

### 5. Настройка Railway проекта

#### A. Основной бот:

1. **New Project** → **Deploy from GitHub repo**
2. Выберите репозиторий
3. Railway определит Dockerfile автоматически
4. Дождитесь сборки

#### B. Добавление базы данных:

1. В проекте нажмите **New**
2. **Database** → **Add PostgreSQL**
3. Railway создаст БД
4. Переменная `DATABASE_URL` добавится автоматически

#### C. Переменные окружения:

Перейдите в **Settings** → **Variables** и добавьте:

| Переменная | Значение | Пример |
|------------|----------|--------|
| `BOT_TOKEN` | Токен от BotFather | `123456:ABCdef...` |
| `ADMIN_IDS` | Ваш Telegram ID | `123456789` |
| `SOLANA_RPC_URL` | RPC endpoint | `https://api.devnet.solana.com` |
| `MASTER_WALLET_PUBLIC_KEY` | Публичный ключ | `AbC123...` |
| `MASTER_WALLET_PRIVATE_KEY` | Приватный ключ | `xyz789...` |
| `MIN_DEPOSIT_SOL` | Минимальный депозит | `0.01` |
| `IMAGE_PRICE_SOL` | Цена по умолчанию | `0.05` |
| `WITHDRAWAL_FEE_PERCENT` | Комиссия вывода | `2` |

**Примечание**: `DATABASE_URL` уже будет добавлен автоматически после создания PostgreSQL!

#### D. Монитор транзакций (Worker):

1. В главном проекте: **New** → **Empty Service**
2. **Settings** → **Source** → выберите тот же GitHub репо
3. **Settings** → **Start Command**: `python monitor_transactions.py`
4. **Variables** → скопируйте все переменные из основного сервиса

### 6. Инициализация базы данных

Один раз после деплоя:

**Вариант A - Railway CLI** (рекомендуется):

```bash
# Установка Railway CLI
npm i -g @railway/cli

# Login
railway login

# Link проекта
railway link

# Выполнение команды
railway run python init_db.py
```

**Вариант B - Временный контейнер**:

1. В Railway → Service → Settings
2. Временно измените Start Command на: `python init_db.py && python main.py`
3. Дождитесь деплоя
4. Верните Start Command обратно на: `python main.py`

**Вариант C - Локально с подключением к Railway БД**:

```bash
# Получите DATABASE_URL из Railway
# Экспортируйте локально
export DATABASE_URL="postgresql://..."

# Запустите
python init_db.py
```

---

## 🎛 Управление на Railway

### Просмотр логов:

1. Railway Dashboard → ваш сервис
2. **Deployments** → выберите активный деплой
3. **View Logs**

Или через CLI:
```bash
railway logs
```

### Перезапуск:

1. Dashboard → Settings → **Restart**

Или через CLI:
```bash
railway restart
```

### Обновление кода:

```bash
# Внесите изменения в код
git add .
git commit -m "Update"
git push

# Railway автоматически задеплоит новую версию!
```

### Переменные окружения:

Изменить в **Settings** → **Variables**

После изменения Railway автоматически перезапустится.

---

## 💰 Стоимость

Railway предлагает:

- **$5 бесплатных кредитов** при регистрации (для тестирования)
- **Hobby Plan**: $5/месяц
  - 500 часов работы
  - $0.000231/GB-hour RAM
  - Достаточно для одного бота

**Для этого бота**: ~$5-7/месяц

---

## 📊 Мониторинг

### Проверка работы:

1. **Railway Dashboard** → Resources
   - CPU usage
   - Memory usage
   - Network traffic

2. **Логи**:
   ```bash
   railway logs --follow
   ```

3. **Метрики**:
   - Dashboard → Metrics
   - Графики использования ресурсов

### Проверка в Telegram:

1. Найдите бота
2. `/start` - должно прийти приветствие
3. `/admin` - админ-панель (если вы админ)

---

## 🔧 Решение проблем

### Бот не отвечает:

```bash
# Проверьте логи
railway logs

# Проверьте переменные
railway variables

# Перезапустите
railway restart
```

### База данных не подключается:

1. Проверьте что PostgreSQL создан
2. Проверьте переменную `DATABASE_URL`
3. Убедитесь что init_db.py выполнен

### Ошибки при деплое:

1. Проверьте логи сборки
2. Убедитесь что `Dockerfile` корректен
3. Проверьте что все зависимости в `requirements.txt`

### Monitor не работает:

1. Убедитесь что создан отдельный Worker сервис
2. Проверьте Start Command: `python monitor_transactions.py`
3. Проверьте что переменные скопированы

---

## ⚙️ Оптимизация для Railway

### 1. Используйте PostgreSQL

Railway предоставляет PostgreSQL бесплатно в Hobby плане.

```env
# Автоматически создается Railway
DATABASE_URL=postgresql://postgres:password@host:5432/railway
```

### 2. Volumes для изображений

Railway поддерживает volumes:

1. Settings → Volumes → Add Volume
2. Mount Path: `/app/images`

### 3. Переменные окружения

Храните все секреты в Variables, а не в коде:
- `BOT_TOKEN`
- `MASTER_WALLET_PRIVATE_KEY`
- И другие

### 4. Healthcheck

Railway автоматически проверяет здоровье сервиса.

Если нужно кастомный healthcheck - добавьте endpoint в `main.py`:

```python
from aiohttp import web

async def health(request):
    return web.Response(text="OK")

app = web.Application()
app.router.add_get('/health', health)
```

---

## 🔄 CI/CD

Railway автоматически:
- ✅ Деплоит при каждом push в GitHub
- ✅ Создает preview deployments для PR
- ✅ Rollback при ошибках
- ✅ Environment variables management

---

## 📈 Масштабирование

Если нужно больше ресурсов:

1. **Pro Plan**: $20/месяц
   - Unlimited hours
   - Priority support
   - Advanced metrics

2. **Увеличение replicas**:
   ```json
   // railway.json
   {
     "deploy": {
       "numReplicas": 2
     }
   }
   ```

---

## ✅ Чеклист развертывания

- [ ] Git репозиторий создан
- [ ] Код запушен в GitHub
- [ ] Railway аккаунт создан
- [ ] Проект на Railway создан
- [ ] PostgreSQL база добавлена
- [ ] Переменные окружения добавлены:
  - [ ] BOT_TOKEN
  - [ ] ADMIN_IDS
  - [ ] SOLANA_RPC_URL
  - [ ] MASTER_WALLET_PUBLIC_KEY
  - [ ] MASTER_WALLET_PRIVATE_KEY
- [ ] Основной бот задеплоен
- [ ] Worker (monitor) задеплоен
- [ ] База данных инициализирована (init_db.py)
- [ ] Бот отвечает в Telegram
- [ ] Админ-панель работает

---

## 🎉 Готово!

Ваш бот работает 24/7 на Railway.app!

### Следующие шаги:

1. **Получите тестовые SOL** (devnet):
   - https://faucet.solana.com/
   - Вставьте ваш MASTER_WALLET_PUBLIC_KEY

2. **Добавьте товары**:
   - `/admin` → ➕ Добавить товар

3. **Пригласите пользователей**:
   - Поделитесь ссылкой на бота

4. **Мониторьте**:
   - Railway Dashboard
   - Логи
   - Статистику в боте

---

## 🔗 Полезные ссылки

- **Railway Docs**: https://docs.railway.app/
- **Railway Templates**: https://railway.app/templates
- **Railway Discord**: https://discord.gg/railway
- **Railway Status**: https://status.railway.app/

---

## 💡 Советы

1. **Тестируйте локально** перед push в GitHub
2. **Используйте .gitignore** для исключения .env
3. **Делайте коммиты** с понятными сообщениями
4. **Проверяйте логи** регулярно
5. **Мониторьте расходы** в Dashboard
6. **Делайте бэкапы** базы данных

---

## 🆘 Поддержка

**Проблемы с Railway**:
- Railway Discord
- Railway Support
- Документация Railway

**Проблемы с ботом**:
- Проверьте `README.md`
- Смотрите логи: `railway logs`
- Создайте Issue на GitHub

---

**Версия**: 1.0.0  
**Платформа**: Railway.app  
**Статус**: ✅ Production Ready

