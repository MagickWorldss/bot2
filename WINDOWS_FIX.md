# 🔧 Решение проблемы с Python на Windows

## 🎯 Проблема

У вас установлено **две версии Python**:
- Python 3.13.5
- Python 3.12

Когда вы пишете `python` - запускается Python 3.13  
Когда вы пишете `pip` - он устанавливает в Python 3.12

Из-за этого возникает ошибка `ModuleNotFoundError`.

---

## ✅ Решение - Готовые скрипты!

Я создал для вас **удобные .bat файлы**, которые используют правильную версию Python!

### Файлы созданы:

| Файл | Назначение |
|------|-----------|
| `setup.bat` | Установка зависимостей + создание кошелька |
| `init.bat` | Инициализация базы данных |
| `run.bat` | Запуск основного бота |
| `run_monitor.bat` | Запуск монитора транзакций |

---

## 🚀 Используйте скрипты

### 1. Установка (первый раз):

Просто **двойной клик** на файл:
```
setup.bat
```

Или в PowerShell:
```powershell
.\setup.bat
```

Это:
- ✅ Обновит pip
- ✅ Установит все зависимости
- ✅ Создаст Solana кошелек

### 2. Инициализация БД (после создания .env):

```
init.bat
```

Или:
```powershell
.\init.bat
```

### 3. Запуск бота:

```
run.bat
```

Или:
```powershell
.\run.bat
```

### 4. Запуск монитора (в другом окне):

```
run_monitor.bat
```

---

## 📝 Ваш кошелек уже создан!

```
Public Key:  2Nx5XkW8cTQ392g61pR31C4iHQrhEXhQLmhp73ZmbaiC
Private Key: 5oDZp4rxjuxEsmVhJbk9rJLaTh4amNj6NPDR4DGMiByy2HAFZG5wutMK9be3TTsybZ15Z2FRe9NNHuScjTApBKVA
```

**⚠️ ВАЖНО**: Сохраните эти ключи в безопасном месте!

---

## 🎯 Следующие шаги

### 1. Создайте .env файл

Создайте файл `.env` в папке `C:\dev\tg\bot2`:

```env
# Telegram
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id

# Database
DATABASE_URL=sqlite+aiosqlite:///./bot.db

# Solana
SOLANA_RPC_URL=https://api.devnet.solana.com
MASTER_WALLET_PUBLIC_KEY=2Nx5XkW8cTQ392g61pR31C4iHQrhEXhQLmhp73ZmbaiC
MASTER_WALLET_PRIVATE_KEY=5oDZp4rxjuxEsmVhJbk9rJLaTh4amNj6NPDR4DGMiByy2HAFZG5wutMK9be3TTsybZ15Z2FRe9NNHuScjTApBKVA

# Settings
MIN_DEPOSIT_SOL=0.01
IMAGE_PRICE_SOL=0.05
WITHDRAWAL_FEE_PERCENT=2
```

**Замените**:
- `ваш_токен_от_BotFather` - на токен от @BotFather
- `ваш_telegram_id` - на ваш ID от @userinfobot

### 2. Инициализируйте базу

```
init.bat
```

### 3. Запустите бота

Откройте **два** окна PowerShell:

**Окно 1**:
```
run.bat
```

**Окно 2**:
```
run_monitor.bat
```

### 4. Проверьте в Telegram

Найдите бота и отправьте `/start`

---

## 💡 Для Railway.app

Когда будете разворачивать на Railway - там эта проблема не будет!

Railway использует Docker с правильной версией Python.

Просто следуйте инструкции **WINDOWS_RAILWAY_GUIDE.md**

---

## 🆘 Если всё равно не работает

Используйте **Python 3.12 напрямую**:

```powershell
# Вместо: python create_wallet.py
# Используйте:
C:\Users\Smokier\AppData\Local\Programs\Python\Python312\python.exe create_wallet.py
```

Или установите только Python 3.12 (удалите 3.13).

---

**Проблема решена!** ✅  
Теперь используйте .bat скрипты!

