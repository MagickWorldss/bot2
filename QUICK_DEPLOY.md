# ⚡ Быстрое развертывание на хостинге

## 🎯 Цель
Запустить бота на сервере за 5 минут.

## 📋 Что вам нужно

1. **VPS/сервер** с Ubuntu (DigitalOcean, Hetzner и т.д.)
2. **SSH доступ** к серверу
3. **Настроенный .env** файл

## 🚀 Метод 1: Автоматический (самый быстрый)

### На локальной машине:

```bash
# 1. Загрузите файлы на сервер
chmod +x upload_to_server.sh
./upload_to_server.sh

# Введите:
# - IP адрес сервера
# - SSH пользователя
# - Путь на сервере
```

### На сервере:

```bash
# 2. Подключитесь
ssh root@ваш_ip

# 3. Перейдите в папку
cd /root/bot2

# 4. Создайте .env
nano .env
# Вставьте настройки (BOT_TOKEN, ADMIN_IDS и т.д.)
# Ctrl+X для сохранения

# 5. Запустите
chmod +x deploy.sh
./deploy.sh
# Выберите режим 1
```

**Готово!** Бот работает 🎉

## 🐳 Метод 2: Docker (рекомендуется)

### Подготовка сервера (один раз):

```bash
# Подключитесь к серверу
ssh root@ваш_ip

# Установите Docker
curl -fsSL https://get.docker.com | sh

# Установите Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
```

### Развертывание бота:

```bash
# Клонируйте или загрузите проект
git clone https://github.com/ваш_username/bot2.git
cd bot2

# Создайте .env
nano .env
# Вставьте настройки

# Запустите
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматически:
- ✅ Проверит Docker
- ✅ Создаст директории
- ✅ Соберет образы
- ✅ Запустит контейнеры
- ✅ Инициализирует БД

## 📱 Метод 3: Railway.app (самый простой, без VPS)

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. New Project → Deploy from GitHub
3. Выберите репозиторий
4. Variables → добавьте из .env
5. Deploy!

**Готово через 2 минуты!**

## 🔧 Метод 4: Ручная установка (без Docker)

```bash
# На сервере
cd /root
git clone https://github.com/ваш_username/bot2.git
cd bot2

# Python и зависимости
apt update
apt install python3.11 python3.11-venv python3-pip -y
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Настройка
nano .env
# Вставьте настройки

# Инициализация
python init_db.py

# Запуск в фоне
nohup python main.py > bot.log 2>&1 &
nohup python monitor_transactions.py > monitor.log 2>&1 &

# Проверка
tail -f bot.log
```

## ⚙️ Минимальный .env для старта

```env
# Обязательные
BOT_TOKEN=токен_от_BotFather
ADMIN_IDS=ваш_telegram_id
MASTER_WALLET_PUBLIC_KEY=публичный_ключ
MASTER_WALLET_PRIVATE_KEY=приватный_ключ

# Опциональные (есть значения по умолчанию)
SOLANA_RPC_URL=https://api.devnet.solana.com
DATABASE_URL=sqlite+aiosqlite:///./bot.db
MIN_DEPOSIT_SOL=0.01
IMAGE_PRICE_SOL=0.05
WITHDRAWAL_FEE_PERCENT=2
```

## 🎛 Управление после запуска

### Docker:

```bash
# Логи
docker-compose logs -f bot

# Статус
docker-compose ps

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Обновление
git pull
docker-compose down
docker-compose build
docker-compose up -d
```

### Без Docker:

```bash
# Логи
tail -f bot.log

# Остановка
pkill -f main.py
pkill -f monitor_transactions.py

# Запуск
nohup python main.py > bot.log 2>&1 &
nohup python monitor_transactions.py > monitor.log 2>&1 &
```

## 🌐 Рекомендуемые хостинги

### Для новичков:
- **Railway.app** ($5/мес) - самый простой
- **Render.com** ($7/мес) - автоматическое развертывание

### Для опытных:
- **DigitalOcean** ($6/мес) - лучший выбор ⭐
- **Hetzner** (€4/мес) - дешевле
- **AWS/GCP** (от $5/мес) - если уже используете

## 📊 Сравнение методов

| Метод | Время | Сложность | Цена | Рекомендация |
|-------|-------|-----------|------|--------------|
| Railway | 2 мин | ⭐ | $5 | Новички |
| Docker | 5 мин | ⭐⭐ | $6 | **Лучший** ⭐ |
| Ручная | 10 мин | ⭐⭐⭐ | $6 | Для изучения |
| Автоматический | 5 мин | ⭐ | $6 | Быстро |

## ✅ Проверка работы

После запуска:

1. **Проверьте логи**
   ```bash
   docker-compose logs -f bot
   # или
   tail -f bot.log
   ```

2. **Найдите бота в Telegram**
   - По username из BotFather

3. **Отправьте /start**
   - Должно прийти приветствие

4. **Проверьте админ-панель**
   - `/admin` → должны видеть меню

5. **Проверьте монитор**
   ```bash
   docker-compose logs -f monitor
   ```

## 🆘 Если что-то пошло не так

### Бот не отвечает
```bash
# Проверьте логи
docker-compose logs bot

# Проверьте что контейнер запущен
docker-compose ps

# Перезапустите
docker-compose restart bot
```

### Ошибка подключения
```bash
# Проверьте .env файл
cat .env

# Проверьте Solana подключение
docker-compose exec bot python test_connection.py
```

### База данных не работает
```bash
# Пересоздайте БД
docker-compose exec bot python init_db.py
```

## 💡 Полезные советы

1. **Используйте screen/tmux** для сеансов SSH
   ```bash
   apt install screen -y
   screen -S bot
   # Ваши команды
   # Ctrl+A, D для отсоединения
   screen -r bot  # для возврата
   ```

2. **Настройте автозапуск** (с Docker это автоматически)

3. **Делайте бэкапы** регулярно
   ```bash
   tar -czf backup_$(date +%Y%m%d).tar.gz data/ images/ .env
   ```

4. **Мониторьте ресурсы**
   ```bash
   htop  # или
   docker stats
   ```

5. **Используйте приватный RPC** для Solana в продакшене

## 📚 Дополнительная информация

- **Полная документация**: `README.md`
- **Детальное развертывание**: `HOSTING_GUIDE.md`
- **Настройка Solana**: `SOLANA_SETUP.md`
- **Развертывание на продакшен**: `DEPLOYMENT.md`

## 🎉 Поздравляем!

Ваш бот теперь работает на хостинге 24/7! 🚀

**Следующие шаги:**
1. Пополните мастер-кошелек SOL
2. Добавьте товары через `/admin`
3. Поделитесь ботом с пользователями
4. Мониторьте логи и статистику

Удачи! 💪

