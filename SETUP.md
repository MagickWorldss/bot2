# Пошаговая настройка бота

## Шаг 1: Подготовка окружения

### Windows

```powershell
# Проверка версии Python (требуется 3.11+)
python --version

# Создание виртуального окружения
python -m venv venv

# Активация
venv\Scripts\activate

# Обновление pip
python -m pip install --upgrade pip

# Установка зависимостей
pip install -r requirements.txt
```

### Linux/Mac

```bash
# Проверка версии Python
python3 --version

# Создание виртуального окружения
python3 -m venv venv

# Активация
source venv/bin/activate

# Обновление pip
pip install --upgrade pip

# Установка зависимостей
pip install -r requirements.txt
```

## Шаг 2: Создание .env файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# ===================================
# Telegram Bot Configuration
# ===================================

BOT_TOKEN=ВАШ_ТОКЕН_ЗДЕСЬ
ADMIN_IDS=ВАШ_ID_ЗДЕСЬ

# ===================================
# Database Configuration
# ===================================

DATABASE_URL=sqlite+aiosqlite:///./bot.db

# ===================================
# Solana Configuration  
# ===================================

SOLANA_RPC_URL=https://api.devnet.solana.com
MASTER_WALLET_PUBLIC_KEY=ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ_ЗДЕСЬ
MASTER_WALLET_PRIVATE_KEY=ВАШ_ПРИВАТНЫЙ_КЛЮЧ_ЗДЕСЬ

# ===================================
# Application Settings
# ===================================

MIN_DEPOSIT_SOL=0.01
IMAGE_PRICE_SOL=0.05
WITHDRAWAL_FEE_PERCENT=2
```

### Получение BOT_TOKEN

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Введите название бота (например: "My Shop Bot")
5. Введите username бота (должен заканчиваться на "bot", например: "myshop_bot")
6. Скопируйте полученный токен
7. Вставьте в `.env` как значение `BOT_TOKEN`

**Пример токена**: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567`

### Получение ADMIN_IDS

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID (числовое значение)
4. Вставьте в `.env` как значение `ADMIN_IDS`

**Пример ID**: `123456789`

Если админов несколько: `123456789,987654321,555666777`

### Получение Solana ключей

Выполните команду:

```bash
python create_wallet.py
```

Программа создаст новый кошелек и выведет:
- Public Key (публичный ключ)
- Private Key (приватный ключ)

Скопируйте их в `.env`:
- `MASTER_WALLET_PUBLIC_KEY` = Public Key
- `MASTER_WALLET_PRIVATE_KEY` = Private Key

**⚠️ ВАЖНО**: Никогда не делитесь приватным ключом!

## Шаг 3: Проверка настройки

Запустите тест подключения:

```bash
python test_connection.py
```

Вы должны увидеть:

```
✓ Public key валиден
✓ Private key валиден
✓ Ключи совпадают
✓ Подключение успешно
✓ Баланс успешно получен
✓ Blockhash получен
```

Если все тесты прошли - настройка завершена!

## Шаг 4: Получение тестовых SOL

Для работы с devnet нужны тестовые SOL.

### Способ 1: Веб-фаусет

1. Откройте [https://faucet.solana.com/](https://faucet.solana.com/)
2. Вставьте ваш `MASTER_WALLET_PUBLIC_KEY`
3. Выберите "Devnet"
4. Нажмите "Confirm Airdrop"
5. Подождите 10-20 секунд

### Способ 2: CLI (если установлен Solana CLI)

```bash
solana airdrop 2 ваш_публичный_ключ --url https://api.devnet.solana.com
```

### Проверка баланса

Снова запустите:
```bash
python test_connection.py
```

Должно показать баланс больше 0.

## Шаг 5: Инициализация базы данных

```bash
python init_db.py
```

Эта команда:
- Создаст все необходимые таблицы
- Добавит 12 регионов ЕС
- Добавит крупные города в каждом регионе

Вы увидите:

```
✓ Created region: Germany
  ✓ Created city: Berlin
  ✓ Created city: Munich
  ...
✅ Database initialization complete!
```

## Шаг 6: Запуск бота

### Вариант А: Два отдельных процесса (рекомендуется)

**Терминал 1 - Основной бот:**

```bash
# Активируйте виртуальное окружение если еще не активно
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Запуск бота
python main.py
```

Вы увидите:
```
INFO - Creating database tables...
INFO - Database tables created successfully
INFO - Starting bot...
```

**Терминал 2 - Монитор транзакций:**

```bash
# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Запуск монитора
python monitor_transactions.py
```

Вы увидите:
```
INFO - === Starting transaction monitor ===
INFO - Starting deposit monitoring...
INFO - Starting withdrawal monitoring...
```

### Вариант Б: Фоновый режим (Linux/Mac)

```bash
# Запуск в фоне
nohup python main.py > bot.log 2>&1 &
nohup python monitor_transactions.py > monitor.log 2>&1 &

# Просмотр логов
tail -f bot.log
tail -f monitor.log

# Остановка
pkill -f main.py
pkill -f monitor_transactions.py
```

## Шаг 7: Первый запуск

1. Найдите вашего бота в Telegram (по username из BotFather)
2. Отправьте `/start`

Вы должны увидеть приветственное сообщение с вашим кошельком.

### Проверка функций

**Пользовательские функции:**
- `📍 Выбрать регион` → выберите регион и город
- `💰 Мой баланс` → просмотр баланса
- `🛍 Каталог` → просмотр товаров (пока пусто)
- `ℹ️ Помощь` → справка

**Админ функции:**
- `/admin` → открыть админ-панель
- `➕ Добавить товар` → загрузить изображение
- `📊 Статистика` → просмотр статистики
- `🗂 Управление регионами` → управление локациями

## Шаг 8: Добавление первого товара

1. В боте отправьте `/admin`
2. Нажмите `➕ Добавить товар`
3. Введите `/1` (выбор первого региона)
4. Введите `/1` (выбор первого города)
5. Отправьте любое изображение
6. Введите цену, например: `0.05`
7. Введите описание или `-` чтобы пропустить

Товар добавлен!

## Шаг 9: Тестовая покупка

### 9.1. Пополнение баланса

Есть два способа:

**Способ A: Реальный перевод через блокчейн**

1. В боте: `💰 Мой баланс`
2. Скопируйте адрес кошелька
3. Используйте Phantom или другой кошелек для перевода SOL
4. Подождите 30-60 секунд
5. Баланс обновится автоматически

**Способ B: Прямое зачисление через базу (только для теста)**

```python
# Создайте файл test_add_balance.py
import asyncio
from database.database import db
from services.user_service import UserService

async def add_test_balance(user_id, amount):
    async for session in db.get_session():
        await UserService.update_balance(session, user_id, amount)
        print(f"Added {amount} SOL to user {user_id}")

# Замените на ваш Telegram ID
asyncio.run(add_test_balance(123456789, 1.0))
```

### 9.2. Покупка товара

1. `📍 Выбрать регион` → выберите регион где добавили товар
2. `🛍 Каталог` → просмотр товаров
3. Нажмите на товар
4. `💳 Купить`
5. `✅ Подтвердить покупку`

Товар будет отправлен вам, а баланс спишется!

## Распространенные проблемы

### Проблема: Import Error

```
ModuleNotFoundError: No module named 'aiogram'
```

**Решение:**
```bash
# Убедитесь что виртуальное окружение активно
# Смотрите в начале строки терминала:
# (venv) - значит активно
# если нет - активируйте

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: Bot не отвечает

**Проверьте:**
1. Запущен ли `main.py`
2. Правильный ли `BOT_TOKEN` в `.env`
3. Есть ли ошибки в терминале
4. Попробуйте `/start` еще раз

### Проблема: Баланс не обновляется

**Проверьте:**
1. Запущен ли `monitor_transactions.py`
2. Правильный ли адрес кошелька
3. Совпадают ли сети (devnet в .env и при переводе)
4. Подождите 30-60 секунд

### Проблема: Ошибка Solana подключения

```bash
# Попробуйте другой RPC
# В .env измените на:
SOLANA_RPC_URL=https://api.devnet.solana.com

# Или используйте альтернативные:
# SOLANA_RPC_URL=https://solana-devnet.g.alchemy.com/v2/demo
```

### Проблема: Database is locked

**Решение:**
```bash
# Остановите все процессы бота (Ctrl+C)
# Удалите lock файлы
rm bot.db-journal
rm bot.db-wal

# Запустите снова
python main.py
```

## Следующие шаги

После успешного запуска:

1. 📖 **Изучите документацию:**
   - `README.md` - полная документация
   - `SOLANA_SETUP.md` - подробно про Solana
   - `DEPLOYMENT.md` - развертывание на сервере

2. 🎨 **Кастомизация:**
   - Измените тексты в `handlers/`
   - Добавьте свои регионы/города
   - Настройте цены в `.env`

3. 🔐 **Безопасность:**
   - Никогда не коммитьте `.env` в git
   - Храните бэкапы `wallet_encryption.key`
   - Используйте сильные пароли

4. 🚀 **Переход на продакшен:**
   - Создайте новый кошелек для mainnet
   - Используйте PostgreSQL
   - Настройте сервер (см. `DEPLOYMENT.md`)
   - Используйте приватный RPC

## Полезные ссылки

- **Solana Explorer (Devnet)**: https://explorer.solana.com/?cluster=devnet
- **Solana Faucet**: https://faucet.solana.com/
- **Solana Docs**: https://docs.solana.com/
- **Aiogram Docs**: https://docs.aiogram.dev/

## Получение помощи

Если возникли проблемы:

1. Проверьте этот файл еще раз
2. Прочитайте `README.md`
3. Запустите `test_connection.py` для диагностики
4. Создайте Issue на GitHub с полным описанием проблемы

---

**Удачи с запуском бота!** 🚀

